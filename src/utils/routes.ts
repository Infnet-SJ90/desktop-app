declare const ROUTE: any;

class SchedulingRoute {
	index: string = ROUTE.INDEX;
	schedulingRequestList: string = ROUTE.SCHEDULING_REQUEST_LIST;
	schedulingList: string = ROUTE.SCHEDULING_LIST;
}

export default new SchedulingRoute();
